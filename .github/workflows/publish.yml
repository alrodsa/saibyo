name: 🚀 Publish Saibyo to GitHub Releases & GCP Artifact Registry

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  publish:
    name: 💠 Build & Upload
    runs-on: ubuntu-latest

    permissions:
      contents: write
      id-token: write  # Needed for federated auth

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: ⚙️ Install uv and publish dependencies
        run: |
          pip install uv
          uv pip install -e ".[publish]"

      - name: 🏷 Extract version
        id: get_version
        run: |
          echo "🔍 Extracting version from pyproject.toml..."
          VERSION=$(python -c "import tomllib; print(tomllib.load(open('pyproject.toml', 'rb'))['project']['version'])")
          echo "VERSION=$VERSION" >> $GITHUB_ENV

      - name: 💠 Build wheel
        run: |
          echo "💠 Building the package..."
          uv build

      - name: 🛡 Ensure wheel exists
        run: |
          ls dist/*.whl || (echo "❌ No wheel found in dist/" && exit 1)

      - name: 📤 Upload to GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ env.VERSION }}
          name: saibyo v${{ env.VERSION }}
          body_path: CHANGELOG.md
          files: |
            dist/*.whl
            src/saibyo/model/weights/flownet.pkl
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: 🔐 Authenticate with Workload Identity Federation
        id: auth
        uses: google-github-actions/auth@v2
        with:
          token_format: access_token
          workload_identity_provider: ${{ secrets.IDENTITY_PROVIDER }}
          service_account: ${{ secrets.IDENTITY_PROVIDER_ACCOUNT }}

      - name: 🚀 Upload to GCP Artifact Registry with uv
        run: |
          echo "🚀 Publishing to Artifact Registry..."
          uv publish --index dev-python \
            --username oauth2accesstoken \
            --password ${{ steps.auth.outputs.access_token }} \
            --keyring-provider subprocess
